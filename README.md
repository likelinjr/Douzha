# Environment Variable Template
Create .env file in the project root directory. The content is same as .env.bak

# Working Directory
The project uses `workingDirectory` as the workspace:
- **Root**: `workingDirectory/`
- **Read/Write**: `workingDirectory/DeskTop/`
- **Read-only**: Documents, Music, Downloads and other folders in `workingDirectory/`